{
  "name": "frametv-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Local relay that lets the LifeOS FrameTV tab upload art to Samsung Frame TVs whose firmware requires the newer D2D-socket Art Mode protocol (not reachable from a browser).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
